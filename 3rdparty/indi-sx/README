SX INDI Driver
==================

This package provides the INDI drivers for SX CCDs, SX wheel and SX Active Optics

Requirtments
============

+ libindi0 >= v0.9.7 (http://indi.sf.net)

	You need to install both libindi0 and libindi0-devel to build this package.
	
+ libcfitsio >= 3.0

	libcfitsio3 is required to compile support for FITS.
	
+ libusb-1.0 >= 1.0.9

  libusb-1.0-dev is required.
  
Installation
============

	See INSTALL
	
How to Use
==========

	You can use the SX INDI Driver in any INDI-compatible client such as OpenPHD, gcx, KStars, Xephem or jINDI. 
	
	To run the driver from the command line:
	
	$ indiserver indi_sx_ccd indi_sx_wheel indi_sx_ao

	You can then connect to the driver from any client, the default port is 7624.
