language: cpp
compiler: gcc
language: cpp
sudo: required
dist: trusty
os: linux


branches:
    only:
        - travis

before_install:
    - echo Install dependencies
    - sudo apt-get -qq update
    - sudo apt-get -y install libusb-1.0-0-dev libcfitsio3-dev libnova-dev libgphoto2-dev libgps-dev libjpeg-dev libopenal-dev

install:
    - mkdir -p build/libindi
    - cd build/libindi
    - cmake . ../../libindi/
    - make
    
script:
    - echo Test
    

