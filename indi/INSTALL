INDI Library Setup 1.0
======================

You must have CMake >= 2.6 in order to build this package.

1) $ tar -xzf indi.tar.gz
2) $ mkdir indi_build
3) $ cd indi_build
4) $ cmake -DCMAKE_INSTALL_PREFIX=/usr . ../indi
5) $ su -c 'make install' or sudo make install
   
Refer to README for instructions on running indiserver and device drivers.

Refer to README.drivers for driver-specific information.

Dependencies
============

+ libusb
+ libnova
+ libfli
+ cfitsio

You can download packages for the dependencies above from SUSE BuildService:

http://software.opensuse.org/search

The packages are available to all major distributions.
